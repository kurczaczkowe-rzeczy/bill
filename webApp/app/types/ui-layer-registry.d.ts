// biome-ignore lint/suspicious/noEmptyInterface: this is a registry for all ui layer types, initially is empty but every component has own registration into this interface
export interface UiLayerRegistry {}
